import { Check, Circle } from 'lucide-react';
import { cn } from '@/lib/utils';
export function JobTimeline({ status }: { status: 'queued' | 'processing' | 'complete' | 'failed' }) { const steps = [{ key: 'queued', label: 'Queued' }, { key: 'processing', label: 'Processing' }, { key: 'complete', label: 'Complete' }]; return <div className="grid gap-3 md:grid-cols-3">{steps.map((step, index) => { const done = status === 'complete' ? true : status === 'processing' ? index < 2 : index === 0; const active = status === step.key; return <div key={step.key} className={cn('rounded-2xl border p-4', active ? 'border-yellow-300 bg-yellow-50' : done ? 'border-emerald-200 bg-emerald-50/40' : 'border-zinc-200 bg-white')}><div className="flex items-center gap-3"><div className={cn('flex h-8 w-8 items-center justify-center rounded-full', done ? 'bg-emerald-500 text-white' : active ? 'bg-yellow-300 text-zinc-900' : 'bg-zinc-100 text-zinc-500')}>{done ? <Check className="h-4 w-4" /> : <Circle className="h-4 w-4" />}</div><div><p className="text-sm font-semibold text-zinc-900">{step.label}</p><p className="text-xs text-zinc-500">{active ? 'Current stage' : done ? 'Finished' : 'Waiting'}</p></div></div></div>; })}</div>; }