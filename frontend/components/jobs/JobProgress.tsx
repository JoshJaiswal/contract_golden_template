export function JobProgress({ status }: { status: 'queued' | 'processing' | 'complete' | 'failed' }) { const value = status === 'queued' ? 20 : status === 'processing' ? 65 : status === 'complete' ? 100 : 100; return <div className="rounded-2xl border border-zinc-200 bg-white p-4"><div className="mb-2 flex items-center justify-between text-sm"><span className="font-medium text-zinc-900">Pipeline progress</span><span className="text-zinc-500">{value}%</span></div><div className="h-2 overflow-hidden rounded-full bg-zinc-100"><div className="h-full rounded-full bg-yellow-300 transition-all" style={{ width: `${value}%` }} /></div></div>; }