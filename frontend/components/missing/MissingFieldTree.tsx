import { MissingFieldCard } from './MissingFieldCard';
export function MissingFieldTree({ groups }: { groups: { name: string; items: { field: string; hint?: string; suggested?: string }[] }[] }) { if (!groups.length) return <div className="rounded-2xl border border-dashed border-zinc-200 p-6 text-sm text-zinc-500">No missing fields detected.</div>; return <div className="space-y-4">{groups.map((group) => <div key={group.name} className="space-y-3"><h3 className="font-semibold text-zinc-900">{group.name}</h3><div className="space-y-3">{group.items.map((item) => <MissingFieldCard key={item.field} {...item} />)}</div></div>)}</div>; }