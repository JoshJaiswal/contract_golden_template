import { Card, CardBody } from '@/components/ui/card';
export function MetricsPanel({ total, complete, processing, failed }: { total: number; complete: number; processing: number; failed: number }) { const metrics = [{ label: 'Total jobs', value: total }, { label: 'Complete', value: complete }, { label: 'Processing', value: processing }, { label: 'Failed', value: failed }]; return <div className="grid gap-4 md:grid-cols-4">{metrics.map((m) => <Card key={m.label}><CardBody><p className="text-xs font-mono uppercase tracking-[0.24em] text-zinc-500">{m.label}</p><p className="mt-2 font-[family-name:var(--font-syne)] text-3xl font-bold text-zinc-900">{m.value}</p></CardBody></Card>)}</div>; }